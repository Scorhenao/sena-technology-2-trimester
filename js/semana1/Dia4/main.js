// Tipos de variables
var data = 6; // mutable
var data = "hola";

let data2 = 0; // mutable pero detecta la duplicacion de asignaciones en nombres de variables

const data3 = 0; // inmutable
console.log(data);

// operadores logicos 
// +, -, *, /, %, //, **, ++, --, ==, ===, !=, !==, >, <, >=, <=, &&, ||, !
//+= concatena ++ iteracion

